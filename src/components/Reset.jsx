export default function Reset({ onReset }) {
  return (
    <button
      className="rounded-md bg-[#6C5EF1] px-8 py-1 text-lg text-white"
      onClick={onReset}
    >
      Reset
    </button>
  )
}
